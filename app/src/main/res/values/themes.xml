<?xml version="1.0" encoding="utf-8"?>
<resources>


    <style name="Theme.Nuerd" parent="Theme.Material3.DayNight">
        <!-- Base application theme attributes -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>

    <style name="Theme.Nuerd.NoActionBar" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>

    <style name="Theme.Nuerd.SplashScreen" parent="Theme.SplashScreen">
        <!-- Make system splash screen transparent/invisible -->
        <item name="windowSplashScreenBackground">@android:color/transparent</item>
        <item name="windowSplashScreenAnimatedIcon">@android:color/transparent</item>
        <item name="windowSplashScreenAnimationDuration">0</item>
        <!-- Your regular theme -->
        <item name="postSplashScreenTheme">@style/Theme.Nuerd.NoActionBar</item>
    </style>

</resources>